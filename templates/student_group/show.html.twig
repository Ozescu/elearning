{% extends 'base.html.twig' %}

{% block title %}StudentGroup{% endblock %}

{% block body %}
    <h1>StudentGroup</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ student_group.id }}</td>
            </tr>
            <tr>
                <th>Approved</th>
                <td>{{ student_group.approved ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_student_group_index') }}">back to list</a>

    <a href="{{ path('app_student_group_edit', {'id': student_group.id}) }}">edit</a>

    {{ include('student_group/_delete_form.html.twig') }}
{% endblock %}
